// MUI
import { CircularProgress } from "@mui/material";

const Loader = () => {
  return (
    <div className="p-4 text-center">
      <CircularProgress />
    </div>
  );
};

export default Loader;
